Question 3.1 : Configuration de SSH

Pour desactiver le ssh sur root on tape les commandes suivantes :
nano /etc/ssh/sshd_config

on remplace le PermitRootLogin avec no

puis on relance le service ssh avec : systemctl restart sshd


Question 3.2 : Authentification cryptographique
